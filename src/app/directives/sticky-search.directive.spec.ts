import { StickySearchDirective } from './sticky-search.directive';

describe('StickySearchDirective', () => {
  it('should create an instance', () => {
    const directive = new StickySearchDirective();
    expect(directive).toBeTruthy();
  });
});
